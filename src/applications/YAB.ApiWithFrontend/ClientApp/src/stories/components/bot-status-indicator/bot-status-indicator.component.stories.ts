import { Meta, Story } from '@storybook/angular';
import { BotStatusIndicatorComponent } from './bot-status-indicator.component';

export default {
  title: "Basics / Essentials / Logo",
  component: BotStatusIndicatorComponent
} as Meta;

export const Basic: Story = () => ({
  props: {},
});
